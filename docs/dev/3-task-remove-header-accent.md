# 移除标题栏蓝色横线 轻量任务记录

> 文档元数据
> - 文件编号：3
> - 文档类型：task
> - 文件路径：docs/dev/3-task-remove-header-accent.md
> - 文档版本：v1.0.0
> - 最后更新：2026-05-27
> - 需求级别：L2
> - 关联需求：去掉“安得合众”下面的蓝色横线。

## 1. 目标

- 要解决的问题：移除标题栏中“安得合众”下方的蓝色装饰横线。
- 成功标准：该蓝色横线不再绘制；其它布局、文字和交互不变；程序可构建。

## 2. 背景与边界

- 背景：当前界面在标题栏底部绘制一条蓝色强调线。
- 包含：删除对应 GLES 矩形绘制调用和不再使用的颜色变量。
- 不包含：修改标题文字、标题栏分隔线、表单、按钮或输入逻辑。
- 关键假设：用户指的是“安得合众”所在标题栏底部的蓝色装饰线，不是标题栏与内容区之间的灰色分隔线。
- 非目标：不调整其它视觉样式。
- 最大修改范围：`src/main.c`、`docs/dev/`。
- 禁止触碰范围：不修改 `/data/source/kmscube`；不触碰未跟踪 `.codex`。

## 3. 风险门禁

| 项 | 结论 |
|----|------|
| 风险矩阵 | L2：C 代码样式调整，运行时风险低。 |
| 高风险开发门禁 | 是：涉及 C 逻辑，但只删除绘制调用。 |
| 破坏性操作 | 否。 |
| 用户已有修改 | 是：未跟踪 `.codex` 不触碰。 |
| 底层/系统风险 | 低：不改 DRM/KMS、GBM、EGL 初始化或 page flip。 |
| 命令权限 | C0/C1；提交阶段限定文件暂存。 |
| 用户确认事项 | 无。 |
| 回滚/止损方式 | 回退本次提交即可。 |

## 4. 方案

- 推荐方案：删除 `render_ui()` 末尾绘制蓝色装饰线的 `draw_rect` 调用，并移除 `accent` 变量。
- 取舍理由：最小改动，精确满足需求，不影响其它控件。
- 风险与应对：通过 `make` 和 `git diff --check` 验证。

## 5. 执行计划

| 步骤 | 修改内容 | 验证方式 | 状态 |
|------|----------|----------|------|
| 1 | 创建任务文档和索引 | 人工检查编号与内容 | 完成 |
| 2 | 删除蓝色横线绘制代码 | `make` | 完成 |
| 3 | 验证并提交 | `git diff --check`、`git status --short` | 完成 |

## 6. 实现记录

- 修改文件：`src/main.c`、`docs/dev/README.md`、`docs/dev/3-task-remove-header-accent.md`。
- 关键决策：只删除标题栏蓝色装饰线的绘制调用，并移除不再使用的 `accent` 颜色变量。
- 计划偏差：无。
- 安全门禁执行结果：未触碰 `/data/source/kmscube`；未运行会接管真实显示输出的 KMS 程序；未触碰未跟踪 `.codex`。

## 7. 验证记录

- 验证环境：本地工作区 `/data/code/tty-ui`。
- 系统信息（OS/内核/架构/编译器/运行时，按需）：沿用 docs/dev/1-task-diskcrypt-kms-ui.md 记录的本地环境。

| 验证项 | 命令/步骤 | 结果 | 备注 |
|--------|-----------|------|------|
| 构建验证 | `make` | 通过 | 无编译警告。 |
| Diff 检查 | `git diff --check` | 通过 | 未发现空白或补丁格式问题。 |

- 未执行验证项：未在真实 TTY/initramfs 上运行 `build/tty-ui -D /dev/dri/card0`，避免在当前环境切换真实显示输出。
- 残余风险：目标设备实际视觉效果未在本轮运行验证。

## 8. 总结

- 最终结果：已移除“安得合众”下方的蓝色装饰横线。
- 遗留风险：无代码层面已知风险；实机显示仍需目标环境确认。
- 后续建议：在目标 TTY/initramfs 环境确认最终视觉效果。
